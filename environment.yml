name: spline_dev
channels:
  - conda-forge
dependencies:
  - bump2version
  - python  # gh runner will pin automatically
  - numpy <1.24
  - cmake =3.25
  - ninja
  - setuptools
  - pybind11 >=2.8
  - gcc =10
  - gxx =10
